# PROJECT_PLAN — «اُلا برازیل» (Olá Brasil)

اپلیکیشن آموزش پرتغالی برزیلی برای فارسی‌زبانان، با تمرکز ویژه بر زندگی روزمره در برزیل و آمادگی زبانیِ فرایند شهروندی.

> این سند نقشهٔ راه و فهرست کارهاست. وضعیت لحظه‌به‌لحظه در `PROGRESS.md` ثبت می‌شود.

## مخاطب و هدف

- کاربران: یک زوج فارسی‌زبان ساکن برزیل، سطح ~A1، هدف زندگی روزمره + شهروندی.
- زبان رابط: فارسی (پیش‌فرض، RTL) — قابل تغییر به پرتغالی و انگلیسی.
- پلتفرم تحویل‌شدهٔ قابل‌اثبات: **PWA موبایل‌محور** (نصب‌شدنی، آفلاین). مسیر Native در `ARCHITECTURE.md`.

## تصمیم‌های کلان (خلاصه؛ کامل در ARCHITECTURE.md)

| حوزه         | انتخاب                                | چرا                                                                                      |
| ------------ | ------------------------------------- | ---------------------------------------------------------------------------------------- |
| فریم‌ورک     | Vite + React 18 + TypeScript          | در این محیط واقعاً build/test/screenshot می‌شود؛ کامپوننت‌ها قابل انتقال به React Native |
| مسیریابی     | React Router v6                       | استاندارد، سبک                                                                           |
| State        | Zustand                               | سبک، بدون boilerplate                                                                    |
| اعتبارسنجی   | Zod                                   | type-safe، مشترک بین محتوا و فرم‌ها                                                      |
| دیتابیس محلی | Dexie (IndexedDB)                     | آفلاین‌محور واقعی، بدون سرور                                                             |
| i18n + RTL   | i18next + dir سوییچ                   | fa/pt/en، جهت‌دهی درست                                                                   |
| SRS          | پیاده‌سازی FSRS                       | استاندارد روز مرور فاصله‌دار                                                             |
| AI           | لایهٔ Provider-Agnostic + Mock        | بدون کلید هم کار می‌کند؛ OpenAI/Anthropic/Gemini قابل اتصال                              |
| TTS          | لایهٔ Provider-Agnostic + Web Speech  | پیش‌فرض رایگان مرورگر (pt-BR)                                                            |
| تست          | Vitest + Testing Library + Playwright | unit/integration/e2e واقعی                                                               |

## مراحل (Milestones)

- [x] M0 — بررسی محیط، اسناد برنامه/معماری
- [ ] M1 — اسکلت اپ: Vite+React+TS، PWA، i18n/RTL، تم روشن/تاریک، ناوبری
- [ ] M2 — مدل داده و لایهٔ دیتابیس محلی (Dexie) + Seed
- [ ] M3 — Design System (کامپوننت‌های پایه، دسترس‌پذیر، RTL)
- [ ] M4 — Onboarding: انتخاب زبان/هدف + تعیین سطح + برنامهٔ شخصی
- [ ] M5 — داشبورد + مسیر آموزشی + صفحهٔ درس + پخش صوت
- [ ] M6 — فلش‌کارت + موتور SRS (FSRS)
- [ ] M7 — ماژول شهروندی: معرفی شخصی، سؤالات مصاحبه، شبیه‌ساز، جملات اداری، چک‌لیست مدارک، محتوای شهر، منابع
- [ ] M8 — لایهٔ AI (Mock + Provider) + مکالمه + شبیه‌ساز مصاحبه
- [ ] M9 — تلفظ (پخش/ضبط/بازپخش) + Shadowing
- [ ] M10 — پیشرفت/آمار + تنظیمات + پروفایل خانوادگی + Export/Delete
- [ ] M11 — Content Pipeline + تولید/اعتبارسنجی محتوای اولیهٔ واقعی
- [ ] M12 — تست‌ها (unit/integration/e2e) + رفع خطا
- [ ] M13 — امنیت/حریم‌خصوصی/عملکرد + مستندات نهایی + گزارش

## فرضیات ثبت‌شده

1. بدون کلید API واقعی شروع می‌کنیم؛ AI و TTS با Mock/Web Speech کار می‌کنند و کلیدهای واقعی از `.env` خوانده می‌شوند.
2. داده‌های حساس شهروندی به‌صورت پیش‌فرض فقط محلی (IndexedDB) ذخیره می‌شوند؛ همگام‌سازی ابری اختیاری و مستند است.
3. اطلاعات حقوقی/مهاجرتی «اطلاعات عمومی، نه مشاورهٔ حقوقی» است و هر ادعا با منبع و تاریخ بررسی همراه می‌شود.
4. هدف‌های تعداد محتوا (۱۰۰۰ واژه و…) اهداف حداکثری‌اند؛ در این نسخه زیرمجموعه‌ای واقعی و باکیفیت + Pipeline تولید ارائه می‌شود و تعداد دقیق گزارش می‌گردد.
